pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'vroomvroom-msa'

include 'common-lib', 'user-service', 'hub-service','order-service', 'gateway-service', 'eureka-server'