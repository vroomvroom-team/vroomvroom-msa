pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'vroomvroom-msa'

include 'common-lib', 'user-service', 'hub-service','order-service', 'delivery-service',
        'gateway-service', 'eureka-server', 'company-service'

